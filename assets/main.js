var game = new Phaser.Game(40, 490, Phaser.AUTO, 'game-window');

var mainState = {
	preload: function() {

	},

	create: function() {

	},

	update: function() {

	},
};

game.state.add('main', mainState);
game.state.start('main')